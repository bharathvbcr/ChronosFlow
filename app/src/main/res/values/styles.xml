<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.ChronosFlow" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowBackground">@drawable/chronos_launch_window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>
    </style>

    <!-- Brand cold-start splash (androidx.core SplashScreen API). Backed by the same cream the
         first Compose surface uses, so the hand-off into the app is seamless. -->
    <color name="chronos_splash_background">#F6F4EF</color>

    <style name="Theme.ChronosFlow.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/chronos_splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_chronosflow_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.ChronosFlow</item>
    </style>
</resources>
